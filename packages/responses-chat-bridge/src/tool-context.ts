/** Compatibility implementation is owned by the shared package; preserve this import contract. */
export {
  ChatBridgeToolContext
} from '@cindy/model-compat';
export type {
  ChatBridgeToolKind,
  ChatBridgeToolSpec
} from '@cindy/model-compat';
