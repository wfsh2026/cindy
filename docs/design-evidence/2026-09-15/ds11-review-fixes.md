# DS-11：Cindy 皮肤复核修订与逐项验收

用户于 2026-09-15 授权合并处理复核修复、自己 review，并补齐原问题 HTML 的修改方案、结果与实际前后对照；Cindy UI 效果优先。最终视觉批准仍由用户逐条验收，不把代码通过测试写成视觉批准。

基线：DS-10 PR #4427，`97c9ecb30062b00a60270815dc4b19296ec047fb`（采集时 open）。DS-11 分支 `ds/11-cindy-review-fixes`；本文件与代码同一提交记录候选版本。DS-10 尚未合入时，PR 需明确依赖关系。

当前范围（2026-09-16）：N7 移动端 Sheet 减弱动效按用户决定撤出，DS-11 不再包含 `apps/mobile` 改动。以下 N7 行仅保留范围裁决，旧截图与测试记录只作历史，不计当前交付。其余 24 项继续逐项审核。

## 逐项处理

| 原编号 | 方案 | 结果 / 边界 |
| --- | --- | --- |
| old-01 | MarketCard、安装位置弹窗和资源用量文字改用字号变量；卡片由固定高度改为最小高度。 | 默认字号保留原角色；UI 18 档卡片标题由 16→20px、正文由13→17px。默认/大号两组对照，固定行框同步改为等比行高。 |
| old-02 | 不同字号名称仍保留独立语义，不按同值删除公开接口。 | 这条复核后属于可选维护收敛，未发现必须修改的可见缺陷；本次不做机械去重。对照标准控件外观未改。 |
| old-03 | 文档补齐确认标题18/500、表单标题18/600等已有裁决。 | 不同场景各自保持层级。普通确认与 MCP 表单标题均有亮暗前后对照，没有把所有标题强行加粗。 |
| old-04 | 不全库取整；资源用量区域的固定 px 行高改为原比例。 | 历史比例仍是原几何的精确换算。示例卡片前后保持；大字体的资源文字不再被旧固定行框约束。 |
| old-05 | 明确 Input 32/36/40px 与 Button 32/36px 的合法配对。 | 保留已接受的尺寸；size 标签差异不再被写成实现错误。 |
| old-06 | 将普通按钮描述改为固定32/36px、横向24px、垂直居中。 | 删除“上下各10px”的矛盾要求。实际控件几何未改；前后图证明未借修文档改变皮肤。 |
| old-07 | 提问卡的6/8/10/16px改用已有1.5/2/2.5/4档，保留必要光学偏移。 | 等值替换，Cindy 文字位置、卡片留白保持；不把扫描命中次数当作缺陷数。 |
| old-08 | 模型与跟随行圆角10→8px、横向padding10→12px，匹配同组菜单合同。 | 选中底、文字与内容角色保持；前后图可看文字起点和选中框圆角。 |
| old-09 | WorkLouder、Storage局部动作补状态/focus；安装弹窗取消复用Button。 | 不全库重写 Settings。钉钉42px、ComputerUse28px等专用几何没有证据要求改动；本图钉钉前后保留。 |
| old-10 | 发送按钮与MCP行内图标动作加soft焦点环；MCP仍用既有内层圆角。 | 焦点进入发送按钮后可见轮廓；鼠标未聚焦状态和特殊发送禁用40%保持。 |
| old-11 | WorkLouder和Storage普通动作使用enabled:hover，Storage统一普通禁用60%。 | 实测恢复默认按钮：禁用状态悬停前后背景相同。发送、Switch已裁决特殊透明度保持。 |
| old-12 | ErrorBanner普通错误分支使用error背景/边框/文字，圆角归8px内层；命名色扫描只增候选报告。 | Cindy亮暗错误底色与文字走同一角色。OpenAI过期专用中性分支保留，门禁未扩大阻断。 |
| old-13 | 新增form-field-hint，默认转发secondary-mid；One Dark Pro/Solarized仅此角色补对比度。 | 11主题的surface-elevated配对均达到4.5:1。Cindy原色保持，亮5.25:1/暗9.16:1；没有全局加深tertiary。 |
| old-14 | tertiary说明移除placeholder；send-btn-disabled-*标为旧接口，当前SendButton仍用40%透明度。 | 没有因未消费就删Token或突然接入旧覆盖。发送按钮禁用效果保持；对照中另一个标准按钮只是焦点参照。 |
| old-15 | 提问卡transition-all改为opacity/transform，使用motion-base和move缓动。 | 桌面系统减弱动效原本有效，本次仍保留；没有把历史审查误判写成修复结果。 |
| old-16 | 普通确认默认Cancel；显式主动作/输入型确认按既有分支。 | 修正文档never Cancel冲突。普通确认实际行为保留，前后取消按钮均获得焦点。 |
| N1 | 默认取消、Tab圈定、Esc/遮罩关闭、返回入口；安装期间禁止重复及关闭。 | 测试与浏览器确认焦点留在弹窗，Esc后关闭并回到入口。增加取消脚部；安装业务回调保持。 |
| N2 | 卡片标题用原生button，Enter/Space打开同一详情；卡片鼠标动作保留。 | Tab先到标题，再到Clone/管理；没有把内嵌按钮包进另一个button。新增标题为无框文本动作。 |
| N3 | 转发aria-invalid/required/describedby；错误边框与焦点使用error角色。 | 组合实验中invalid/required从缺失变为true；错误文案关联保留，不启用新的业务校验。 |
| N4 | hover与focus分别计入暂停原因，全部离开后按剩余时长继续。 | 同样聚焦操作按钮并等待8.5秒：修改前已移除，修改后通知仍在且焦点保留。 |
| N5 | 整体留16px视口边距；来源、URL、动作可完整显示。实际换行外框12px，短通知仍pill。 | 800px窗口中按钮不再被裁切；来源保留并单独截断，正文允许断行，操作文字完整；短/长通知均实测。 |
| N6 | 只在Markdown渲染器设置绝对font-bold，保留内容语义与主题strong颜色。 | 同一嵌套示例：外层700不变，内层900→700。没有全局改写UI chrome的strong。 |
| N7（已撤出） | 2026-09-16 用户逐项审核决定：移动端 Sheet 减弱动效暂不修改，从 DS-11 移除。 | 共用 hook 恢复 main 基线实现，同时移除本批专属测试；未声称问题已修复。后续如需处理，单独安排移动端实施及原生验证。 |
| N8 | 范围下拉旁增加有标签的标准date Input；和图表使用相同选日状态。 | 输入/图表互相同步；清空回30天、禁止未来日期。独立审查追加修复加载窗口：日期基准未就绪时禁用输入，读取后启用；亮暗浏览器验证未加载→加载→未来拒绝→2026-09-10选中，有新增状态图。原生日期弹层外观仍由系统浏览器决定。 |

## 实际截图与可复核方法

交付 HTML：`documents/2026-09-15-cindy-design-review.html`（本地交付，不入 Git）；原报告备份为同目录 `.before-ds11.html`。原 5e663ba9fd 报告提供 24 项、82 张独立图片、116 处展示，包含实际入口、Cindy Light / Dark 切换、逐图放大与本地审核标记；自包含且保持在手机传输预算内。大图 WebP 压缩；原始 PNG 与 SHA-256 清单保留。修改前为 DS-10 候选，修改后为本批源码。资源用量组件源码未变，其补拍的修改前图用基线 CSS 在同一真实组件上复原，报告已明确标注。

采证目录：`/var/folders/yh/nrp07sr92331p22044y8zl440000gn/T/cindy-ds11-1qetn0p8`，包含 `before/`、`after/`、`screenshots-manifest.json`、脚本、日志。此目录是当前机器的临时证据，不能当作公开持久链接。附件按治理 §6 由报告/本地 artifact 交付；GitHub 图片上传需要网页附件，尚未有可写回的公开评论 URL。

场景直接 import 生产组件与 globals/ThemeService，在真实浏览器中执行焦点、键盘、计时、字号设置、主题切换；宿主接口及数据为虚构数据，禁止外部请求，不触真实安装、配置或消息。主题切换至少等 450ms、等待字体后采集。此证据不冒充完整 Electron 构建截图，Mobile 源码截图也不冒充原生 UI。完整 Desktop 整页目检、Mobile 原生亮暗/读屏仍未验证（当前无可用 iOS 模拟器设备）；按 DESIGN §10 与治理 §6 的 best-effort 条款如实申报。

## 自动与运行检查（N7 撤出前的历史结果）

- `pnpm test:unit:related`：原候选通过；主干合并后执行 `VITE_CINDY_AUTH_REGION=global pnpm test:unit:related`，自动回退全量，runner 与 29 个工作区全部通过。Global 仅作用于测试进程，避免本机 CN 配置影响默认路径断言；未改产品区域配置。旧 Markdown 源码断言已按内容 strong 700 合同修订，新增 Token 分类账同步为 542 项，均纳入此次完整重跑。
- 原候选 Desktop、Mobile、`@cindy/design-tokens` typecheck 及主干合并后受影响包 typecheck：通过。
- `pnpm --filter @cindy/design-tokens check:generated` 与 `pnpm check:design-inventory`：通过，50 个 surface；生成器没有读取或重刷视觉预期。
- i18n key / glossary：通过；现存翻译与 proposed 术语告警保留。
- 焦点：安装初始 Cancel、Tab 不逃逸、Esc 关闭后返回入口；忙碌时禁止关闭/重复操作；市场标题成为原生详情动作。
- 通知：hover / focus 独立暂停剩余计时，两种离开顺序有测试；800px 长 URL / 来源 / 操作未越过视口。
- Select：辅助状态、错误文案关联及错误 focus 环；日期输入与图表共享选日，清空回30天、拒绝未来日期。
- Markdown 嵌套 strong：700 / 700；大字号下 MarketCard / 资源文字随设置放大。
- 合并主干后的生产组件运行复查：Select 必填/错误状态、安装取消初始焦点与 Esc 返回入口、800px 长通知范围通过。
- HTML 在 390×844 手机与 1280×900 桌面检查：24 项有证据，图片全部解码，展开全部详情没有横向溢出，亮暗切换、跳转、原始尺寸放大、本地审核标记通过；新增日期状态图随报告再次核验。
- Mobile JS 生命周期测试通过；没有原生依赖、配置或模块改动。本地 fingerprint 结果存在，但未声称已做 base/main 对比；合并结果以 CI guard 为准。

## 帮助文字配对

仅以下 surface-elevated 配对被测，不能泛化为整个主题已验收。Cindy 原色与 U2 Token 值不改；新独立角色允许局部覆盖。One Dark Pro / Solarized 的独立预期只增加新 hint 角色，未更改原有主题字段。

| 主题 | 前景 | 背景 | 对比度 |
| --- | --- | --- | --- |
| default-light | rgb(82, 82, 82) | rgb(255, 255, 255) | 7.81:1 |
| default-dark | rgb(163, 163, 163) | rgb(44, 44, 42) | 5.55:1 |
| cindy-light | rgb(107, 107, 103) | rgb(253, 253, 248) | 5.25:1 |
| cindy-dark | rgb(193, 193, 193) | rgb(31, 31, 31) | 9.16:1 |
| one-dark-pro | rgb(139, 144, 154) | rgb(33, 37, 43) | 4.80:1 |
| github-dark | rgb(125, 133, 144) | rgb(22, 27, 34) | 4.64:1 |
| eclipse | rgb(156, 163, 175) | rgb(22, 27, 34) | 6.81:1 |
| material-ocean-hc | rgb(166, 172, 205) | rgb(9, 11, 16) | 8.81:1 |
| monokai-pro | rgb(193, 192, 192) | rgb(34, 31, 34) | 8.99:1 |
| atom-one-light | rgb(105, 108, 119) | rgb(255, 255, 255) | 5.23:1 |
| solarized-light | rgb(104, 104, 104) | rgb(238, 232, 213) | 4.55:1 |

## 规范、兼容与回滚

Toast 短通知保留 pill；实际消息或动作换行时按内容容器取 12px，依据用户以 Cindy 实際 UI 效果优先的本轮指令。所有状态色、默认时长与调用接口保留。模型菜单归一8px/12px，普通按钮几何、发送40%透明度、MCP内层圆角及公开字号别名保留。`send-btn-disabled-*` 未删除或强行接入。

新增 `form-field-hint` 默认转发 `text-secondary-mid`；外部主题导入模板不扩展、已有主题文件不改写，用户可显式覆盖新帮助角色。命名 Tailwind 色只新增 candidate report，不扩大阻断范围。

本批可按视觉/交互消费者及对应 Token 源与生成输出整体 revert；不涉及协议、数据库、系统提示词或用户数据迁移。G2 独立非设计师试用仍未发生，Mobile 设计系统完整迁移仍为后续阶段。

## 结合 main 的实际窗口复查（2026-09-15）

本轮将 main `b201f1f663a1199c1e296ee0b6ddca7d465d4e9d` 与 DS-11
`5e663ba9fd5204cfb3dea5f8ef797b35e09981ba` 合并；纯合并树为
`513da4ced9c5ea8db97095f54ea062a975ffdb14`。57 个设计文件与 40 个 main 更新文件
无重叠，逐文件 blob 校验均保留。main 的飞书 / Ghost / SQL 0107 更新来自上游，
本设计批未修改这些行为；数据库只在独立沙箱初始化。

实际 App 新发现 N9：顶层 `useDisableTab` 全局阻止 Tab / Shift+Tab，
使设置字号输入的失焦提交失效，也阻断 DS-11 的键盘入口。
本次移除唯一调用与废弃 hook；编辑器补全、权限切换与模态焦点循环仍由各组件处理。
§14.2 补明 App 根层必须保留普通遍历。此前组件场景未挂载 App，这一缺口不能用
旧的组件测试通过来否认。

运行采用安全包装入口 `restart:desktop:remote --region=global --isolated=ds11-review --passive`，
进入未登录本地模式。主会话在实际 Electron 窗口检查 Cindy Light / Dark 的新建页、
外观设置、界面字号 14 / 18 和用量日期，8 张整页图；N9 另有两模式修改前后 4 张图。
字号输入 18 后 Tab 从原来的原地不动变为到达代码字体入口，`--text-13` 由 13px 变为 17px，
Shift+Tab 可返回。标准模态窗口另检 Tab / Shift+Tab 遍历、圈定、Esc 与入口焦点返回。
日期证据等待输入框与范围下拉同步后采集：历史日选中、未来拒绝、清空回 30 天。
旧固定等待截图出现过中间态，已单独归档，不再用作当前日期状态证明。

本轮原始 PNG、运行脚本和 JSON 存于临时证据目录
`/var/folders/yh/nrp07sr92331p22044y8zl440000gn/T/cindy-ds1011-main-review-3e937tta`。
原自包含 HTML 追加为 25 项、94 张独立图、128 处展示；保留原 24 项和浏览器审核标记。
新增真实窗口证据与原生产组件场景分别标注，不把源码图当作 Mobile 真机图。

最终验证记录随本轮结果回填；GLM / Terra 对结合 main 的有效差异分别独立审查，
本地测试和实际运行由主会话执行。原生 Mobile / 读屏、Windows 字体回退、完整业务场景
与 G2 独立非设计师试用仍未覆盖，用户最终视觉验收待完成。

### 收尾时同步更新后的 main

最终整合基线更新为 `40162c508ff10b0f5d3398fd4af5db82dd39f319`。
DS-10 已随 #4427 以 `2afecd89e5` 合入；相对该 main，DS-11 与 Tab 修复的有效差异
为 55 个文件。三处合并冲突保留 DS-10 间距分类与 DS-11 palette 候选报告，
并回填设计系统索引、治理中的系列状态；50 个 surface 台账按最终源码重生成。
模型菜单的上游账号来源信息与本批 8px 圆角、12px 横向内边距同时保留，五语言日期词条无丢失。

`latest-main/` 另存整合后的 8 张整页截图、模型菜单亮暗 2 张截图、字号 Tab 与模态运行记录。
模型菜单两模式均有 4 个来源行，实际计算圆角 8px、内边距 8px/12px，无视口横向溢出；
字号与日期检查通过。模态焦点停在关闭按钮且其 Tooltip 打开时，第一下 Esc 关闭提示，
第二下关闭模态并返回入口；不能将该嵌套浮层情形写成单次 Esc 关闭整个弹窗。
当前开发窗口同时包含工作区中另一任务的蓝色 Switch 修改；本 DS 暂存树及独立测试快照
不含该修改，原前后对照基线保留。模型菜单截图含本机账号显示信息，仅交付本地报告，不上传公开附件。

旧 b201 的截图与测试记录继续作为历史证据保留，不充当 401 最新结果。
此前门禁分别出现超时、线程崩溃、Node 原生 ABI 不匹配和依赖目录缺文件；
在隔离目录重新安装依赖后，Vite、TypeScript 与 SQLite 内存数据库检查通过，
最终门禁结果如下。无断言、超时或测试范围被弱化以制造通过。

最终自动验证：在基于 main `40162c508f` 的隔离 checkout、源码树 `a9dc063332` 上，
Node 22.22.3 下 `VITE_CINDY_AUTH_REGION=global pnpm test:unit:related` 退出 0：
runner 542 通过 / 1 跳过，Desktop 96.4 秒、Mobile 4.0 秒、design-tokens 22.0 秒均通过。
相关包类型检查通过（8 个包纳入命令，实际提供 typecheck 的 Desktop、Mobile、design-tokens 均执行）；
Token 生成、50 个 surface 台账与颜色增量检查通过（unexpected=0）。这是本次相关范围通过，
不是此前全库失败被抹去。该轮此后仅补充本证据文字。该时点 HTML 为 25 项、96 张独立图片、130 处展示。


## 2026-09-16：按用户决定撤出 N7

- 恢复 `apps/mobile/src/session/useModalFadeLifecycle.ts` 至本批 main 基线 `40162c508f` 的内容，移除 DS-11 新增的 `modalFadeLifecycle.test.tsx`。该测试专属已撤回的功能，不再作为本 PR 的实现合同；其余既有测试不变。
- 当前有效差异不再包含 Mobile 文件；Desktop 的字号跟随、Tab 修复与其它皮肤修改保留。
- 本地报告移除 N7 的当前审核卡、跳转项和两张源码图，保留其余审核标记与新对比查看器：24 项、94 张独立图、128 处展示。N7 仅以“暂缓、已从 DS-11 撤出”记录，不视为修复或视觉验收通过。
- 此次是范围缩减；此前双审与运行证据绑定其原提交，不冒充撤出后的独立复审。撤出后的自审确认 hook 与 main 基线逐字节一致，整个 PR 的 Mobile 文件差异为空。
- 撤出后的隔离验证树 `039db0ff6699` 执行根级 `VITE_CINDY_AUTH_REGION=global pnpm test:unit:related` 退出 0：runner 542 通过 / 1 跳过，Desktop 84.1 秒、design-tokens 19.8 秒；Mobile 与 main 无差异，本次 related 未选择它，不声称重新跑过已撤出的测试。Desktop、Mobile、design-tokens 类型检查均通过。最终提交相对该验证树仅补本段验证文字。
- HTML 手机 / 桌面复验通过：24 个审核项、128 处图片，N7 卡片及导航已移除；94 张保留图片内容不变，键盘同位切换、分割线、差异高亮、缩放及本地审核标记保留。

## 2026-09-16：同步 main 并解决生成台账冲突

- 将 main `40f087c99c` 合入 N7 已撤出的 DS-11。唯一文本冲突是 `design-inventory.md` 生成区的快照日期，按合并源码重生成；50 个 surface 校验通过，人工区原样保留。
- 47 个未与主干重叠的 DS 文件保持原内容（含删除的全局 Tab hook）；五种语言同时保留主干所有新增内容和 DS 日期词条。相对最新 main，`apps/mobile` 差异仍为空，N7 没有重新引入。
- 在独立验证目录，将同一个合并树 `cacd7554b404` 放在最新 main 基线上执行根级 `VITE_CINDY_AUTH_REGION=global pnpm test:unit:related`，退出 0：runner 543 通过 / 2 跳过，Desktop 84.2 秒、design-tokens 19.9 秒。Desktop、design-tokens 类型检查及 Token 生成物检查通过。最终提交相对被测树只追加本段记录和治理表的正式 PR 状态。
- 首次检查被目录自动选择的 Node 24 与 SQLite 的 Node 22 ABI 不匹配阻断；固定 Node 22.22.3 后，原断言、原超时和同一根级命令通过，没有修改产品或测试来规避失败。
- 用户明确免本地双审；本轮由主会话核对合并保真与执行检查。不把此前独立审查或 main 401 的历史截图冒充本轮重新双审或整页视觉复验；用户已有皮肤审核记录保留。
