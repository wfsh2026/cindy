<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!-- Keep Android's HTTPS default; only the on-device snapshot listener needs HTTP. -->
  <domain-config cleartextTrafficPermitted="true">
    <domain>127.0.0.1</domain>
  </domain-config>
</network-security-config>
