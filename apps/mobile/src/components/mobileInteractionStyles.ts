import { StyleSheet } from 'react-native';

/** Shared press feedback for primitives and custom compact action frames. */
export const mobileInteractionStyles = StyleSheet.create({
  pressed: { opacity: 0.72 },
});
